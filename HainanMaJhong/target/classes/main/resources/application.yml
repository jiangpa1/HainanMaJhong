spring:
  # MySQL 配置
  datasource:
    url: jdbc:mysql://39.107.247.119:3306/hainan_majong?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 10201122Zxj@
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # 启动时不校验连接，避免数据库未启动导致应用起不来（首次查询才真正连接）
      initialization-fail-timeout: -1

  # Redis 配置（Spring Boot 2.7 用 spring.redis 前缀，不是 spring.data.redis）
  # 39.107.247.119
  redis:
    host: 39.107.247.119
    port: 6379
    password: 10201122Zxj@   # 如果虚拟机 Redis 设了密码就填，没设就留空
