spring:
  # MySQL 配置
  datasource:
    url: jdbc:mysql://***SERVER-IP-REMOVED***:3306/hainan_majong?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8
    username: root
    password: ***REMOVED******REMOVED***
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      # 启动时不校验连接，避免数据库未启动导致应用起不来（首次查询才真正连接）
      initialization-fail-timeout: -1

  # Redis 配置（Spring Boot 2.7 用 spring.redis 前缀，不是 spring.data.redis）
  # ***SERVER-IP-REMOVED***
  redis:
    host: ***SERVER-IP-REMOVED***
    port: 6379
    password: ***REMOVED******REMOVED***   # 如果虚拟机 Redis 设了密码就填，没设就留空
